# © Copyright 2026 Aaron Kimball
"""Non-Python data files shipped inside the installed klorb package, read at runtime via
`importlib.resources` (see `klorb.system_prompts.resolve_prompt_file`). Declared as package
data in `pyproject.toml` so they're present in the wheel."""
