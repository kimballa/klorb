
# Klorb scripts

This directory contains scripts for package maintainer / developer use; not intended
for direct invocation by end-users.

* `fetch_embedding_model.py` -- downloads from HuggingFace the embedding model used for
  semantic search and stores it in `src/klorb/resources/`.
