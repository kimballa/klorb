# © Copyright 2026 Aaron Kimball
"""`agents.json` fixture (schema `klorb-agents`) used to test `AgentRegistry` against a
stable, non-production set of subagent role definitions."""

from pathlib import Path

SAMPLE_AGENTS_JSON = Path(__file__).parent / "agents.json"
